# Machine-local / secret files — never commit
.zshrc.local
*.local

# Claude Code — settings, permissions, and project-instruction files
.claude/
CLAUDE.md
CLAUDE.local.md

# Backups created by link.sh
*.backup-*

# macOS cruft
.DS_Store

# iTerm2 writes a lock/aux file alongside its plist in the custom folder
iterm2/*.lock
